<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. -->
<project basedir="." default="dist" name="ShellTest">
     <property name="src" location="src" />
        <property name="dist" location="dist" />
        
   <path id="base.path">
     <fileset dir="/WEB-INF/lib">
               <include name="*.jar" />
                </fileset>
                <fileset dir="${user.home}/.ShellTest/lib">
                        <include name="*.jar" />
                </fileset>
                <pathelement location="${build}/WEB-INF/classes" />
    </path>
      <target name="init">
                <tstamp />
                <mkdir dir="/WEB-INF/classes" />
                <mkdir dir="${dist}" />
        </target>

      <target name="compile" depends="init">
                <javac source="1.7" target="1.7" encoding="GBK" srcdir="${src}"
destdir="/WEB-INF/classes" debug="true" debuglevel="lines,vars,source" includejavaruntime="true">
                        <classpath refid="base.path"/>
                </javac>
       </target>

        <target name="dist" depends="war">
        </target>

        <target name="war" depends="compile">
                <copy todir="/WEB-INF/classes">
                        <fileset dir="${src}">
                                <include name="**/*.xml"/>
                <include name="**/*.dat"/>
                                <include name="**/*.properties"/>
                <include name="**/*.xlsx"/>
                        </fileset>
                </copy>
                <copy todir="/WEB-INF/classes" overwrite="true">
                        <fileset dir="${user.home}/.ShellTest">
                                <include name="*.properties"/>
                                <include name="*.xml"/>
                <include name="**/*.xlsx"/>
                        </fileset>
                </copy>
                <war webxml="/WEB-INF/web.xml" destfile="${dist}/ROOT.war" basedir="."/>
        </target>

        <target name="clean" description="clean up">
              
                <delete dir="${dist}" />
        </target>
</project>
